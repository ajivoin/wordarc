import 'package:flutter/widgets.dart';

class CandidateWordBanner extends StatelessWidget {
  const CandidateWordBanner({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
